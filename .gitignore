
# MacOS folder metadata, not relevant to codebase
.DS_Store


# contains installed node modules and dependencies, which should be managed with npm rather than with version control
# Deno doesn't use node_modules, but we'll ignore it just in case we use it for something parallel to Deno
node_modules

# .env files contain sensitive information like API keys, database passwords, etc. Should be kept private and not tracked in version control for security reasons.
.env

# `out` is a commmon name for a directory containing built or compiled files, like the output of a build process. These files are generated from source and do not need to be tracked.
out

# aider files
.aider*
